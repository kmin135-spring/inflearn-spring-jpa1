# spring boot 기본동작이 h2 mem을 올려주므로 사실 아래 설정을 모두 제거해도 자동으로 h2 mem 기반으로 테스트가 동작한다!
# 즉, yaml 파일만 분리하고 비워놔도 된다는 말이다.

#spring:
#  datasource:
#    # https://www.h2database.com/html/features.html#database_url
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
##        show_sql: true
#        format_sql: true

#logging:
#  level:
#    org.hibernate.SQL: debug
#    org.hibernate.type: trace